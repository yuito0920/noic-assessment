# NOIC 脳アセスメント（本番）

**URL**: https://yuito0920.github.io/noic-assessment/

## 管理者がやること（1回）

1. **Firestore** を未作成なら作成（東京）: [Firestore コンソール](https://console.firebase.google.com/project/noic-assessment/firestore)
2. **ルール** に `firestore.rules` と同じ内容
3. **このリポジトリの [`firebase-config.json`](firebase-config.json) を編集**し、Firebase の `firebaseConfig` を貼る → Commit

HTML は触らない。設定は `firebase-config.json` のみ。

詳細はワークスペースの `公開まで_これだけ_.md` を参照。
